# Catalog: maintained via create-collection workflow (assistant + maintainer + PR review).
# Golden sources: skills/*/SKILL.md, README.md, CLAUDE.md, marketplace/rh-agentic-collection.yml
# Do not edit ad hoc — follow COLLECTION_SPEC.md and the create-collection skill.

id: rh-developer
name: Red Hat Developer Agentic Skills Collection
provider: Red Hat
version: 0.1.0
categories:
- Developer
- Openshift
- Rhel
- S2I
- Containerization
- Deployment
personas:
- Red Hat platform engineer
marketplaces:
- Claude Code
- Cursor
description: Plugins for building and deploying applications on Red Hat platforms.
summary: '- **Pack:** `rh-developer`

  - **Focus:** Plugins for building and deploying applications on Red Hat platforms.

  - **Skills:** see `.catalog/collection.yaml` contents for the authoritative list.

  '
contents:
  description: The pack provides 13 skills and 1 orchestration skills under `rh-developer` for Red Hat platforms.
  skills:
  - name: debug-build
    description: 'Diagnose OpenShift build failures including S2I builds, Docker/Podman builds, and BuildConfig issues. Automates
      multi-step diagnosis: BuildConfig validation, build pod logs, registry authentication, and source reposit...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Diagnose OpenShift build failures including S2I builds, Docker/Podman builds, and BuildConfig issues.
      Automates multi-step diagnosis: BuildConfig validation, build pod logs, registry authentication, and source reposit...'
  - name: debug-container
    description: 'Diagnose local container issues with Podman/Docker including image pull errors, container startup failures,
      OOM kills, and networking problems. Automates multi-step diagnosis: container inspect, logs retrieval, image ...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Diagnose local container issues with Podman/Docker including image pull errors, container startup
      failures, OOM kills, and networking problems. Automates multi-step diagnosis: container inspect, logs retrieval, image
      ...'
  - name: debug-network
    description: 'Diagnose OpenShift service connectivity issues including DNS resolution, service endpoints, route ingress,
      and network policies. Automates multi-step diagnosis: service endpoint verification, pod selector matching, ro...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Diagnose OpenShift service connectivity issues including DNS resolution, service endpoints, route
      ingress, and network policies. Automates multi-step diagnosis: service endpoint verification, pod selector matching,
      ro...'
  - name: debug-pipeline
    description: 'Diagnose OpenShift Pipelines (Tekton) CI/CD failures including PipelineRun failures, TaskRun step errors,
      workspace/PVC binding issues, and authentication problems. Automates multi-step diagnosis: PipelineRun status, ...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Diagnose OpenShift Pipelines (Tekton) CI/CD failures including PipelineRun failures, TaskRun step
      errors, workspace/PVC binding issues, and authentication problems. Automates multi-step diagnosis: PipelineRun status,
      ...'
  - name: debug-pod
    description: 'Diagnose pod failures on OpenShift including CrashLoopBackOff, ImagePullBackOff, OOMKilled, and pending
      pods. Automates multi-step diagnosis: pod status, events, logs (current + previous), and resource constraint anal...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Diagnose pod failures on OpenShift including CrashLoopBackOff, ImagePullBackOff, OOMKilled, and pending
      pods. Automates multi-step diagnosis: pod status, events, logs (current + previous), and resource constraint anal...'
  - name: debug-rhel
    description: 'Diagnose RHEL system issues including systemd service failures, SELinux denials, firewall blocking, and
      system resource problems. Automates multi-step diagnosis: journalctl log analysis, SELinux denial detection (ause...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Diagnose RHEL system issues including systemd service failures, SELinux denials, firewall blocking,
      and system resource problems. Automates multi-step diagnosis: journalctl log analysis, SELinux denial detection (ause...'
  - name: deploy
    description: Create Kubernetes Deployment, Service, and Route resources on OpenShift to deploy and expose an application.
      Use this skill after /s2i-build to make the built image accessible. Handles port detection, replica configur...
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Create Kubernetes Deployment, Service, and Route resources on OpenShift to deploy and expose an application.
      Use this skill after /s2i-build to make the built image accessible. Handles port detection, replica configur...'
  - name: detect-project
    description: Analyze a project folder or GitHub repository to detect programming language, framework, and version requirements.
      Use this skill when containerizing an application, selecting an S2I builder image, deploying to OpenSh...
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Analyze a project folder or GitHub repository to detect programming language, framework, and version
      requirements. Use this skill when containerizing an application, selecting an S2I builder image, deploying to OpenSh...'
  - name: helm-deploy
    description: Deploy applications to OpenShift using Helm charts. Use this skill when user wants to deploy with Helm, when
      a Helm chart is detected in the project, or when /helm-deploy command is invoked. Supports both existing cha...
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Deploy applications to OpenShift using Helm charts. Use this skill when user wants to deploy with
      Helm, when a Helm chart is detected in the project, or when /helm-deploy command is invoked. Supports both existing
      cha...'
  - name: recommend-image
    description: Intelligently recommend the optimal S2I builder image or container base image for a project based on detected
      language/framework, use-case requirements, security posture, and deployment target. Supports GitHub URLs fo...
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Intelligently recommend the optimal S2I builder image or container base image for a project based
      on detected language/framework, use-case requirements, security posture, and deployment target. Supports GitHub URLs
      fo...'
  - name: rhel-deploy
    description: 'CRITICAL: When user types /rhel-deploy, use THIS skill immediately. This skill deploys applications to standalone
      RHEL/Fedora/CentOS systems (NOT OpenShift) using Podman containers with systemd, or native dnf builds. ...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** CRITICAL: When user types /rhel-deploy, use THIS skill immediately. This skill deploys applications
      to standalone RHEL/Fedora/CentOS systems (NOT OpenShift) using Podman containers with systemd, or native dnf builds.
      ...'
  - name: s2i-build
    description: Create BuildConfig and ImageStream resources on OpenShift and trigger a Source-to-Image (S2I) build. Use
      this skill after /detect-project to build container images from source code on the cluster. Handles namespace ve...
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Create BuildConfig and ImageStream resources on OpenShift and trigger a Source-to-Image (S2I) build.
      Use this skill after /detect-project to build container images from source code on the cluster. Handles namespace ve...'
  - name: validate-environment
    description: Check and report the status of required tools and environment for rh-developer skills. Validates tool installation
      (oc, helm, podman, git, skopeo, etc.), cluster connectivity, and permissions. Use this skill before ru...
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Check and report the status of required tools and environment for rh-developer skills. Validates tool
      installation (oc, helm, podman, git, skopeo, etc.), cluster connectivity, and permissions. Use this skill before ru...'
  orchestration_skills:
  - name: containerize-deploy
    description: Complete end-to-end workflow for containerizing and deploying applications to OpenShift or standalone RHEL
      systems. Orchestrates /detect-project, /s2i-build, /deploy, /helm-deploy, and /rhel-deploy skills with user co...
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Complete end-to-end workflow for containerizing and deploying applications to OpenShift or standalone
      RHEL systems. Orchestrates /detect-project, /s2i-build, /deploy, /helm-deploy, and /rhel-deploy skills with user co...'
  skills_decision_guide:
  - user_request: 'User: "I need help related to containerize-deploy"'
    skill_to_use: containerize-deploy
    reason: Use `containerize-deploy` for workflows described in that skill and in CLAUDE.md.
  - user_request: 'User: "I need help related to debug-build"'
    skill_to_use: debug-build
    reason: Use `debug-build` for workflows described in that skill and in CLAUDE.md.
  - user_request: 'User: "I need help related to debug-container"'
    skill_to_use: debug-container
    reason: Use `debug-container` for workflows described in that skill and in CLAUDE.md.
  - user_request: 'User: "I need help related to debug-network"'
    skill_to_use: debug-network
    reason: Use `debug-network` for workflows described in that skill and in CLAUDE.md.
  - user_request: 'User: "I need help related to debug-pipeline"'
    skill_to_use: debug-pipeline
    reason: Use `debug-pipeline` for workflows described in that skill and in CLAUDE.md.
deploy_and_use: '## Install (Lola)


  Add the marketplace and install this module from `marketplace/rh-agentic-collection.yml` (path `rh-developer`). See the
  pack `README.md` for prerequisites, MCP env vars, and safety notes.

  '
sample_workflows:
- name: Choose the right skill
  workflow: 'User: "I have a task for this collection"

    - Open CLAUDE.md intent routing and match your request to a skill name.

    - Invoke that skill and follow its workflow and prerequisites.

    '
resources:
- title: agentic-collections repository
  description: Source repository for these packs and skills.
  url: https://github.com/RHEcosystemAppEng/agentic-collections
repository: https://github.com/RHEcosystemAppEng/agentic-collections
license: Apache-2.0
