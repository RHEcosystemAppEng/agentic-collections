# Catalog: maintained via create-collection workflow (assistant + maintainer + PR review).
# Golden sources: skills/*/SKILL.md, README.md, CLAUDE.md, marketplace/rh-agentic-collection.yml
# Do not edit ad hoc — follow COLLECTION_SPEC.md and the create-collection skill.

id: openshift-virtualization
name: Red Hat OpenShift Virtualization Agentic Collection
provider: Red Hat
version: 0.1.0
categories:
- Red Hat
- Kubevirt
- Openshift
- Virtualization
- Virt
personas:
- Red Hat platform engineer
marketplaces:
- Claude Code
- Cursor
description: Virtual machine management and automation for OpenShift Virtualization and KubeVirt workloads.
summary: '- **Pack:** `rh-virt`

  - **Focus:** Virtual machine management and automation for OpenShift Virtualization and KubeVirt workloads.

  - **Skills:** see `.catalog/collection.yaml` contents for the authoritative list.

  '
contents:
  description: The pack provides 9 skills and 1 orchestration skills under `rh-virt` for Red Hat platforms.
  skills:
  - name: vm-clone
    description: 'Clone existing virtual machines for testing, scaling, or creating templates. Use when: - "Clone VM [source]
      to [target]" - "Create a copy of VM [name]" - "Duplicate VM [name] for testing" - "Create 3 copies of templat...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Clone existing virtual machines for testing, scaling, or creating templates. Use when: - "Clone VM
      [source] to [target]" - "Create a copy of VM [name]" - "Duplicate VM [name] for testing" - "Create 3 copies of templat...'
  - name: vm-create
    description: 'Create new virtual machines in OpenShift Virtualization with automatic instance type resolution and OS selection.
      Use when: - "Create a new VM" - "Deploy a virtual machine with [OS]" - "Set up a VM in namespace [name]...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Create new virtual machines in OpenShift Virtualization with automatic instance type resolution and
      OS selection. Use when: - "Create a new VM" - "Deploy a virtual machine with [OS]" - "Set up a VM in namespace [name]...'
  - name: vm-delete
    description: 'Permanently delete virtual machines and their associated resources from OpenShift Virtualization. Use when:
      - "Delete VM [name]" - "Remove virtual machine [name]" - "Destroy VM [name]" - "Clean up VM [name]" This skil...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Permanently delete virtual machines and their associated resources from OpenShift Virtualization.
      Use when: - "Delete VM [name]" - "Remove virtual machine [name]" - "Destroy VM [name]" - "Clean up VM [name]" This skil...'
  - name: vm-inventory
    description: 'List and view virtual machines across namespaces with status, resource usage, and health information. Use
      when: - "List all VMs" - "Show VMs in namespace [name]" - "What VMs are running?" - "Get details of VM [name]" ...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** List and view virtual machines across namespaces with status, resource usage, and health information.
      Use when: - "List all VMs" - "Show VMs in namespace [name]" - "What VMs are running?" - "Get details of VM [name]" ...'
  - name: vm-lifecycle-manager
    description: 'Manage virtual machine lifecycle operations including start, stop, and restart. Use when: - "Start VM [name]"
      - "Stop the virtual machine [name]" - "Restart VM [name]" - "Power on/off VM [name]" This skill handles VM ...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Manage virtual machine lifecycle operations including start, stop, and restart. Use when: - "Start
      VM [name]" - "Stop the virtual machine [name]" - "Restart VM [name]" - "Power on/off VM [name]" This skill handles VM
      ...'
  - name: vm-snapshot-create
    description: 'Create virtual machine snapshots for backup and recovery. Use when: - "Create a snapshot of VM [name]" -
      "Backup VM [name] before upgrade" - "Take a snapshot of [vm]" Validates storage class snapshot support, CSI driv...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Create virtual machine snapshots for backup and recovery. Use when: - "Create a snapshot of VM [name]"
      - "Backup VM [name] before upgrade" - "Take a snapshot of [vm]" Validates storage class snapshot support, CSI driv...'
  - name: vm-snapshot-delete
    description: 'Permanently delete virtual machine snapshots to free storage space. Use when: - "Delete snapshot [snapshot-name]"
      - "Remove old snapshots for VM [name]" - "Free up snapshot storage" Requires user confirmation before d...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Permanently delete virtual machine snapshots to free storage space. Use when: - "Delete snapshot [snapshot-name]"
      - "Remove old snapshots for VM [name]" - "Free up snapshot storage" Requires user confirmation before d...'
  - name: vm-snapshot-list
    description: 'List virtual machine snapshots across namespaces with status, age, and recovery information. Use when: -
      "List snapshots for VM [name]" - "Show snapshots in namespace [name]" - "What snapshots exist for [vm]?" Read-on...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** List virtual machine snapshots across namespaces with status, age, and recovery information. Use when:
      - "List snapshots for VM [name]" - "Show snapshots in namespace [name]" - "What snapshots exist for [vm]?" Read-on...'
  - name: vm-snapshot-restore
    description: 'Restore virtual machines from snapshots with strict safety confirmations to prevent data loss. Use when:
      - "Restore VM [name] from snapshot [snapshot-name]" - "Roll back VM [name] to snapshot" - "Recover VM [name] fro...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Restore virtual machines from snapshots with strict safety confirmations to prevent data loss. Use
      when: - "Restore VM [name] from snapshot [snapshot-name]" - "Roll back VM [name] to snapshot" - "Recover VM [name] fro...'
  orchestration_skills:
  - name: vm-rebalance
    description: 'Orchestrate VM migrations across cluster nodes for load balancing, maintenance, and resource optimization.
      Use when: - "Move VM database-01 to worker-03" - "Rebalance VMs to optimize CPU load" - "Drain worker-02 for m...'
    summary_markdown: '**Use when:** See the skill description and CLAUDE.md intent routing.


      **What it does:** Orchestrate VM migrations across cluster nodes for load balancing, maintenance, and resource optimization.
      Use when: - "Move VM database-01 to worker-03" - "Rebalance VMs to optimize CPU load" - "Drain worker-02 for m...'
  skills_decision_guide:
  - user_request: 'User: "I need help related to vm-rebalance"'
    skill_to_use: vm-rebalance
    reason: Use `vm-rebalance` for workflows described in that skill and in CLAUDE.md.
  - user_request: 'User: "I need help related to vm-clone"'
    skill_to_use: vm-clone
    reason: Use `vm-clone` for workflows described in that skill and in CLAUDE.md.
  - user_request: 'User: "I need help related to vm-create"'
    skill_to_use: vm-create
    reason: Use `vm-create` for workflows described in that skill and in CLAUDE.md.
  - user_request: 'User: "I need help related to vm-delete"'
    skill_to_use: vm-delete
    reason: Use `vm-delete` for workflows described in that skill and in CLAUDE.md.
  - user_request: 'User: "I need help related to vm-inventory"'
    skill_to_use: vm-inventory
    reason: Use `vm-inventory` for workflows described in that skill and in CLAUDE.md.
deploy_and_use: '## Install (Lola)


  Add the marketplace and install this module from `marketplace/rh-agentic-collection.yml` (path `rh-virt`). See the pack
  `README.md` for prerequisites, MCP env vars, and safety notes.

  '
sample_workflows:
- name: Choose the right skill
  workflow: 'User: "I have a task for this collection"

    - Open CLAUDE.md intent routing and match your request to a skill name.

    - Invoke that skill and follow its workflow and prerequisites.

    '
resources:
- title: agentic-collections repository
  description: Source repository for these packs and skills.
  url: https://github.com/RHEcosystemAppEng/agentic-collections
repository: https://github.com/RHEcosystemAppEng/agentic-collections
license: Apache-2.0
