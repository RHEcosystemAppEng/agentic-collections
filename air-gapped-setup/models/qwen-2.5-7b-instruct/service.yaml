apiVersion: v1
kind: Service
metadata:
  name: llm-qwen-2-5-7b-svc
  namespace: llm-models
  labels:
    app: llm-serving
    model: qwen-2.5-7b-instruct
spec:
  type: ClusterIP
  selector:
    app: llm-serving
    model: qwen-2.5-7b-instruct
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: ClientIP  # Sticky sessions for better performance
